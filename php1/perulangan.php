<?php

    $level = 0;
    $levelMaks = 15;

    //for
    for ($level; $level <= $levelMaks ; $level++) { 
        echo "Level: $level <br>";
    }

    //while
    while ($level < $levelMaks) {
        $level++;
        echo "Level: $level <br>";
    }